sudo: false
language: node_js
node_js:
  - "4"
before_install:
before_install:
  - tar --version
  - sudo apt-get update
  - sudo apt-get install -y tar
  - tar --version
  - git submodule update --init --recursive --rebase
